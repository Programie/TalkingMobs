image: maven:3-jdk-8

cache:
  paths:
    - .m2/repository

maven-build:
  stage: build
  script: mvn compile

maven-test:
  stage: test
  script: mvn test
  dependencies:
  - maven-build

maven-deploy:
  stage: deploy
  script: mvn package
  dependencies:
  - maven-test
  artifacts:
    paths:
    - target/TalkingMobs.jar
  only:
    - master